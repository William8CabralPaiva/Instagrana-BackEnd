import knex from '../database/connection';
import { json, Request, Response } from 'express';

class ChatController {
    async addMessage(request: Request, response: Response) {


    }

}

export default ChatController;